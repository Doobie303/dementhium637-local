# Task handoff: <subsystem / objective>

- Authorized scope and latest owner decisions:
- Relevant reports (only those needed to resume):
- Current implementation / changed files:
- Changed contract, expected result, affected production entry path and decisive reproduction/test:
- Next concrete work and blockers:
- Selected suites and coverage; measured test duration (compile/hash time separate); results/log paths. Automated assertion totals optional and secondary:
- Evidence state: implemented / automated verified / staged / loaded after restart / live accepted (record each separately, with unknown or pending where appropriate):
- Source/runtime identity: batch manifest, backup paths, known later writers or hash drift; never infer loaded classes from timestamps alone:
- Remaining acceptance or reproduced defects: next exact check and the condition that closes it:
- Constraints or deferrals specific to this work:

Keep this to the information needed to resume. Link existing evidence instead of copying it. Separate completed, pending and unverified work. Do not include secrets, broad histories, automatic new-task creation or invented authorization. Save a completed handoff only at a meaningful pause or subsystem boundary.

