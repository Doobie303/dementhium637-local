# Status ledger

## Current state

- NPC combat repair (2026-09-11): the actual packed dataset now has 978 records (399 before; 579 added, 15 existing records repaired); every unselected record is byte-for-byte preserved. Static Attack IDs have profiles for 505/506 (245 before); 8436 remains uncertain. All 78 summonable bases have profiles, including five deliberately non-fighting helpers. After known exclusions, 864 ordinary/conditional Attack profiles plus 73 combat familiar bases form a 937-profile roster, not a claim of complete gameplay verification. Exact changed IDs are in `tests/npc-combat-repaired.txt`; canonical XML comments and the five NPC repair/update scripts under `tools` distinguish preserved data, family inference and explicit server estimates.
- Active repairs cover ordinary mixed dragons/wyverns/revenants/waterfiends, Slayer contact magic/protection/leaf weapons/Gargoyle finishing, Cave horrors, poison creatures, Chinchompa prey, familiar presentation/Hydra/Steel titan sessions, registered dungeon dragon rigs, and simplified ordinary-world Yk'Lagor. Strykewyrm investigation now changes the real ID/definition, with active drops, mixed attacks, avoidable burrow and mound respawn; the option-1 approach event uses its real Y coordinate. Custom NPC loading no longer depends on XML whitespace; definition reload validates slots/truncation before publication. Broad ammunition is registered with compatible bows/crossbows; existing strength is retained. Sixteen affected Java sources are compiled into normal `bin` (Java 8), and ten focused checks pass against that output, including 37,995 definition/registry checks and real packet/session/death paths. Backups are under `build/npc-definition-repair` and `build/npc-*-backup-*`; data generation/repacking is idempotent. No server restart or client acceptance was performed.
- NPC coverage limits remain explicit: of 5,003 cache Attack IDs, 886 have packed records; the other 4,117 comprise 12 script-defined Fight Cave forms, 60 familiar presentation forms, uncertain static NPC 8436, and 4,044 cache-only/unsupported candidates. Four out-of-range spawn IDs and quest-transform parents 4613/4616/4622/4628 remain outside the verified roster. Existing WildyWyrm 3334 and Skeletal Horror 9177-9180 lack complete supported encounter controllers; no generic replacement was applied. Monkey zombies 1465-1467 and Barker toad 6889 still have unresolved combat animation mappings. Inferred family assets require client playback; Yk'Lagor and burrow/gear penalty amounts are server designs, not exact historical reconstructions. Full Daemonheim progression, quest controllers and other deferred features remain outside this repair.

- Bounded server hygiene is source-only: removed the unused `ServerWidePlayerHandler` duplicate and EventManager directory-scanning helpers; XML aliases now share one initialization path. Event and shop loaders close inputs on success/failure. Hint-icon lookup now finds targets beyond empty slots, distinguishes player/NPC indices, and cannot remove an unrelated icon when full; existing coordinate identity and full-capacity replacement remain. Isolated Java 8 checks pass (infrastructure 97, icons 46, existing PvP 9568), with baseline failures for the corrected bugs and identical tested XML bytes. Live bin is untouched, including its obsolete handler class. Client icon acceptance remains pending; optional `PlayersUpdateTask` integration is retained because ownership/intent is uncertain.

- World-tick backlog stabilization is source-only: the existing 600 ms producer now retains one worker and at most one pending tick, coalescing additional wake-ups without interruption or skipped logical updates. The saved baseline reproduced an interrupted operation and three live workers/two World-monitor waiters; corrected probes have zero replacement/interruption, owner mismatch or callback violation. Neither baseline nor corrected tests showed protected World overlap. Six warmed combat trials per variant (216 measured ticks, 8/32 players, ordinary/God Mode and a two-player Nex blood slice) stayed below backlog, with body maxima 15.60/15.09 ms; this does not establish live pause causation or capacity. Fresh isolated Java 8 compilation, native negative control, scheduler probes and focused regressions passed (instance lifecycle 73, Nex lifecycle 260, Nex blood 1437, shared combat 149). Reproduce with tests/run-world-tick.ps1; evidence is under build/world-tick-20260911. Live runtime is unchanged; later in-game acceptance remains required.

- The broader NPC/combat Step 2 remains paused. The separately authorized Nex blood-phase repair is staged and accepted by the owner with godmode on; godmode-off fidelity acceptance remains pending. Do not release unrelated God Wars, Corp, or reward work without renewed direction.
- Owner live testing with godmode on confirms the repaired Nex blood phase and Saradomin behavior. Bandos's distinct smash and melee sequences were also observed; its remaining godmode-off, two-player damage, and active ranged-playback boundaries remain open.
- Nex stabilization is source-only and verified in isolated output: full special recovery/sacrifice deadlines, immediate damage-path minion gates, and cancellation of stale projectile packets. The gate-crossing hit retains its contact effects; God Mode's 750 damage and active-shield rules remain. Owner-approved practical timing now uses three-tick shadow traps and seven-tick sacrifice; sacrifice casts before damage using the ordinary three-tick cast-to-hit interval. Timing/packet/movement regressions failed against the previous source and pass after the change: lifecycle 260, blood/full-fight/respawn 1437, damage 750. Earlier ice, forced movement, arena pursuit and shared damage checks also passed; live runtime was not updated.
- God Wars doorway and room reentry behavior is owner-confirmed fixed in live testing.
- NPC animation/overlap/arena-pursuit work and Piscatoris home are staged with remaining live acceptance. Starfall remains a paused concept; its asset prototype is tests-only, and its statue/native rendering/collision/cache-map work is unproven.
- The developer-client loading artwork is published through the maintained launcher. Cold startup, native crossfade/OpenGL, and composed login/input acceptance remain pending; lobby branding is deferred.
- The OSRS equipment expansion (Primordial, Pegasian and Eternal boots; Avernic defender; Max and Infernal max capes) is implemented, verified, and published through the maintained launcher. Owner live review accepted the Avernic but found boot floor and max-cape shoulder fit issues; the focused correction is verified and published from `build/client-batches/osrs-equipment-fit`, with live fit acceptance pending.
- The capability-gated Staff Tools side tab is implemented, verified, and published through the maintained developer-client launcher, including an optically centered gold administrator crown, clean variable-width categories, native hover-lightening, explicit moderator/admin actions, and an isolated Grand Exchange item picker with clean cancellation. Item spawn now uses a chatbox confirmation with a validated noted-form choice when available. Server restart and live fixed/resizable acceptance remain pending.

- Custom/imported items 20430–20436 share the ordered `ItemSpec` catalog in `src/org/dementhium/content/items/CustomItems.java`; registration, capacity, OSRS grants and capability/fallback rules derive from it. Cache encoding is separate; `InfernalCapeCache` retains only lazy resource-loading semantics, and the old `content.OsrsEquipment.cacheDefinition` remains for the active client proof. Isolated Java 8 checks pass (Infernal 36, OSRS 232, absorption 254); all seven custom definitions/cache bytes and 11 controls match the working baseline, including reload. Cold loading, legacy binary compatibility and invalid-ID rejection pass. Live runtime/assets are unchanged; in-game visual acceptance remains pending.

## Active blockers

- Fight Caves: the Tz-Kek retaliation hit icon is corrected in the working tree with isolated regression coverage; live visual acceptance remains pending. The ground-item refresh ghost is staged with restart and live drop/move/pickup acceptance pending. Local profiles for IDs 2734–2744 remain uncompiled, unstaged, and balance-unaccepted at the owner's request.
- Verify initial-login interface refresh: the player's name does not appear immediately after login, but appears after clicking the minimap, inventory, or another interface element.
- God Wars live attack/follower presentation remains pending. Corp's energy/HP/hop behavior is present in the local runtime and awaits owner live validation; ground-projectile timing is staged with isolated packet/scheduler coverage and awaits live visual acceptance.
- Verify Corporeal Beast projectile impact behavior: a projectile was observed striking the player and producing an explosion effect; confirm that the impact and visual are accurate to the 2011 encounter.
- Verify Dark Energy Core behavior: when spawned, it did not attack the player, followed the player, and moved underneath the player. Confirm its attack, pursuit, and collision/positioning behavior against the 2011 Corp encounter.
- Nex live acceptance remains pending: verify the three-tick shadow warning/burst and seven-tick sacrifice warning/cast/projectile/impact sequence in client playback. Exact 2011 calibration is no longer the owner's acceptance requirement; the retained ordinary four-tick cadence still needs live feel validation. The confirmed pre-Fumus Blood Barrage observation remains unresolved: current smoke-path tests emit projectile 306, not blood projectile 374 or blood specials; visual identity and the live attack source need capture. Spell IDs were preserved.
- Nex Reavers are summoned by siphon, not their deaths. Isolated ordinary/God Mode late-kill tests both permit a new pair four ticks later at the already-due siphon; the stationary rotation now measures 40 ticks between siphons because sacrifice gained two ticks. Ordinary attack spacing, special order and the eight-tick siphon hold are retained. This explains a possible quick replacement without proving the owner's exact observation.
- Wider combat calibration remains open, including formulas, reach/equipment/resources, spell coverage, specials, and delayed callbacks. Elysian over-reduction remains unreproduced; Staff of Light is ruled out.

## Durable boundaries

- Completed audits and implemented repairs are historical evidence, not fresh defect lists; recheck current source before reopening them.
- Further Summoning feature work and full Dungeoneering migration remain deferred; the bounded familiar combat/profile repairs above are implemented. Quest implementation and solo-minigame concepts remain review-only.
- Private friend testing is deferred with two approved options for later review: (1) restrict TCP 43594 to the tester's public IP at the router/Windows Firewall, which is simpler but requires that IP; or (2) add private access-key authentication to both the client and server, which does not require the tester's IP but needs implementation and validation on both sides. No hosting or network exposure is currently authorized.
